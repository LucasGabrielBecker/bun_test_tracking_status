{
    "name": "bun_test_tracking_status",
    "module": "index.ts",
    "type": "module",
    "private": true,
    "scripts": {
        "build": "bun build --minify --target=browser index.html **.ts --outdir dist",
        "start:prod": "bun dist/index.html"
    },
    "devDependencies": {
        "@types/bun": "latest"
    },
    "peerDependencies": {
        "typescript": "^5"
    }
}
